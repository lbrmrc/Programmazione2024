#include <stdio.h>

int main(){
    // definisco la variabile intera v
    int v;
    int doppio;

    // richiedo all'utente il valore di v (input)
    scanf("%d", &v);
    
    doppio = v * 2;

    // stampo il valore di v (output)
    printf("%d\n", doppio);
}